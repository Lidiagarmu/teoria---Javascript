const flag = new Boolean(); // esto es declarar un booleano. Por defecto devuelve false. si en boolean(true) le temo un true devolvera true
// la palabra Boolean --> se refiere al tipo
// si le meto en () un 0 --> (0) muestra TRUE
// si le meto (1) --> muestra FALSE
console.log(flag);

let flagTwo; // escribo let en lugar de const porque las declaraciones const DEBEN INICIALIZARSE

console.log(flagTwo); //muestra undefined porque no esta definido
console.log(flag); // muestra FALSE
