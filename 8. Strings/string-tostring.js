const myAge = 30; // este metodo cambia un tipo de dato a String. En este caso cambia un number a String

const ageString = myAge.toString();
if (typeof ageString === "string") {
  console.log(myAge.toString);
}

//el if (typeof ageString --> es para confirmar si me devuelve un string. No haria falta poner la FILA 6)
