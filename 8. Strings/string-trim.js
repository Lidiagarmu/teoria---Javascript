const greeting = "     Hello      world";

console.log(greeting.trim()); //este metodo elimina los espacios sin sentido. los del principio y los del final

console.log(greeting.trimStart()); // elimina los del principio
console.log(greeting.trimEnd()); // elimina solo los del final
