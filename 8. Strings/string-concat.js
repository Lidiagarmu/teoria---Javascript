const name = "John";
const greeting = "Hello";

//const concatVariables = greeting.concat(" ", name); // la idea del concat. es crear una variable nueva con las 2 variables anteriores sin modificarlas

//const concatVariables = `${greeting} ${name}; --> podria hacerse asi en vez de usar el metodo concat. de la FILA8

console.log(greeting.concat(" ", name)); // --> esto seria para verlo en consola sin necesidad de crear la FILA 4

console.log(concatVariables);
